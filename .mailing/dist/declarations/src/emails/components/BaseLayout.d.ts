import React from "react";
declare type BaseLayoutProps = {
    width?: number;
    style?: string;
    children: React.ReactNode;
};
export default function BaseLayout({ width, children, style, }: BaseLayoutProps): JSX.Element;
export {};
